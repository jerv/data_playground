{
  "name": "data-playground",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "webapp",
    "backend"
  ],
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=16.0.0"
  }
} 