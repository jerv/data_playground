{
  "name": "data-playground",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "webapp",
    "backend"
  ],
  "scripts": {
    "build": "npm run build:webapp && npm run build:backend",
    "build:webapp": "cd webapp && npm install && npm run build",
    "build:backend": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=16.0.0"
  }
} 