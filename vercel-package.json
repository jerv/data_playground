{
  "name": "data-playground-vercel",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "build": "cd webapp && npm install --legacy-peer-deps && npm run build",
    "start": "cd webapp && npm start"
  }
} 